// index.js

import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent ensures the environment is set up correctly,
// whether in Expo Go or a standalone/native build.
registerRootComponent(App);
